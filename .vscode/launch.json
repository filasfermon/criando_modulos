{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Arquivo Atual (Terminal Integrado)",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}",
      "justMyCode": true
    }
  ]
}
